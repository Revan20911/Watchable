
const mongoose = require('mongoose');

const Schema = mongoose.Schema;

const movie = new Schema({

    name: String,
    summary: String,
    genre: String, 
    img: String,
});

const Movie = mongoose.model('Movie', movie, process.env.MOVIES);

module.exports = Movie;